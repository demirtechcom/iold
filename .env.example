# Mock names only. Never commit real values.
IOLD_GATEWAY_URL=https://gateway.staging.example.invalid
IOLD_GATEWAY_TOKEN=replace-with-secret
IOLD_GATEWAY_MODE=mock

IOLD_EXPOSURE_MODE=runpod-proxy
RUNPOD_POD_ID=replace-with-pod-id

# Required only when cloudflare-tunnel is selected.
CLOUDFLARE_TUNNEL_TOKEN=replace-with-secret
